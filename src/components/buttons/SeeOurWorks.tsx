"use client";

import React from "react";

const SeeOurWorks = ({ className }: { className: string }) => {
  return <button className={className}>See Our Works</button>;
};

export default SeeOurWorks;
